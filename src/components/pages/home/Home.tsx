import React, { FC } from "react";
import Greetings from "./greetings/Greetings";

const Home: FC = (): JSX.Element => {
  return (
    <main>
      <Greetings />
    </main>
  );
};

export default Home;
